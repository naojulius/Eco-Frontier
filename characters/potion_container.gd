extends HBoxContainer
const POTION_PANEL = preload("uid://b35qjub8hkkfr")


# Called when the node enters the scene tree for the first time.
func _ready() -> void:
	#for i in get_children():
	#	i.queue_free()
	pass
	
	#for potion in InventoryManager.potions:
	#	var panel: Panel = POTION_PANEL.instantiate()
	#	panel.resource = potion
	#	add_child(panel)
		
	#get_child(0).is_active = true
